import React, { Component } from 'react';
import style from './index.css';

const Title = class Title extends Component {
  render() {
    return (
      <div className={style.title}>
        List of Awesome
      </div>
    );
  }
};

export default Title;
